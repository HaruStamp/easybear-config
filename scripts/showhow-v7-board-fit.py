#!/usr/bin/env python3
# showhow-v7-board-fit.py — prompt บอร์ด (mnBoard..mnBoard6) ไม่โดน engine ตัดท้ายเมื่อมีรูป (2026-09-17 · starter สั่งแทนพี่หมี)
# วัดก่อน (engine จริง · easybear-showhow/scripts/prompt-clamp.py + บทจริง 22 บท): ทะลุ 3,900 = 466/5,535 · สูงสุด 4,194 · โหมดตั้งต้น 6/123
#   ส่วนที่หาย = บล็อกท้าย "คนในคลิป" (เห็นแต่มือ/ล็อกหน้า)
# ① ย้าย "ปิดท้ายทุกภาพด้วย" + "คนในคลิป" ขึ้นก่อน "5 แถว:" — ถ้ายังเกิน ของที่หายคือท้าย VO แถว 5 แทนกฎคน
# ② บีบข้อความโดยไม่ตัดกฎ: PRODUCT LOCK 6 bullet → 3 · LOCATION LOCK · กติกาความต่อเนื่อง · ประโยคฟอนต์ overlay
import json, os
P = os.path.join(os.path.dirname(os.path.abspath(__file__)), '..', 'showhow-dev.json')
raw = open(P, encoding='utf-8').read(); d = json.loads(raw)

REPL = [
    ("กติกาความต่อเนื่อง: **ถ้ามี**บอร์ดของช่วงอื่นในคลิปเดียวกันแนบมาด้วย (รูปท้าย ๆ) — ต้องใช้โทนสี แสง ฉากหลัง พื้นผิว และสไตล์ตัวหนังสือชุดเดียวกันกับบอร์ดนั้นทุกประการ เพื่อให้คลิปที่ต่อกันดูเป็นคลิปเดียว · แต่ **ห้ามลอกภาพในช่องมาซ้ำ** — ฉากในแผงนี้เป็นฉากของตัวเองตามบทด้านล่าง",
     "กติกาความต่อเนื่อง: **ถ้ามี**บอร์ดช่วงอื่นของคลิปนี้แนบมา (รูปท้าย ๆ) ใช้โทนสี แสง ฉากหลัง พื้นผิว สไตล์ตัวหนังสือชุดเดียวกัน แต่ **ห้ามลอกภาพในช่องมาซ้ำ** — ฉากในแผงนี้ทำตามบทด้านล่าง"),
    ("กติกาเหล็ก — ความเหมือนของสินค้า (สำคัญกว่าความสวยของภาพ):\n- สินค้าทุกชิ้นที่ปรากฏต้องตรงกับรูปที่แนบมาแบบเป๊ะ (แนบหลายชิ้น = แต่ละชิ้นตรงกับรูปของตัวเอง) — ถือรูปที่แนบเป็นภาพถ่ายของจริง ไม่ใช่แค่แรงบันดาลใจ\n- ห้ามเปลี่ยน: สี เฉดสี รูปทรง สัดส่วน ขนาด วัสดุ พื้นผิว ฉลาก โลโก้ ตัวอักษรบนสินค้า จำนวนชิ้นและส่วนประกอบ\n- ห้ามวาดใหม่ ห้ามตีความใหม่ ห้ามทำให้เป็นภาพวาดหรือการ์ตูน ห้ามเพิ่มหรือลบรายละเอียดใดของตัวสินค้า\n- สไตล์ภาพและแสงใช้กับฉาก พื้นหลัง เลย์เอาต์เท่านั้น — ตัวสินค้าคงสีและวัสดุจริงตามรูปเสมอ\n- ถ้ามุมกล้องที่บทระบุเป็นมุมที่รูปที่แนบไม่มี ให้ใช้มุมที่ใกล้เคียงที่สุดจากรูปที่แนบ และห้ามเดารายละเอียดด้านที่มองไม่เห็น\n- เอาเฉพาะตัวสินค้าจากรูปที่แนบ — ห้ามลอกฉากหลัง ของประกอบ ของตกแต่ง หรือข้อความโฆษณาที่อยู่ในรูปสินค้ามาใส่ในฉาก\n",
     "กติกาเหล็ก — สินค้าต้องเหมือนรูปที่แนบเป๊ะ (สำคัญกว่าความสวย · หลายชิ้น = แต่ละชิ้นตรงรูปของตัวเอง · ถือเป็นภาพถ่ายของจริง):\n- ห้ามเปลี่ยนสี เฉดสี รูปทรง สัดส่วน ขนาด วัสดุ ฉลาก โลโก้ ตัวอักษร จำนวนชิ้น · ห้ามวาดใหม่ ตีความใหม่ ทำเป็นการ์ตูน หรือเพิ่มลดรายละเอียด\n- สไตล์และแสงใช้กับฉากเท่านั้น ตัวสินค้าคงสีและวัสดุจริง · มุมที่รูปไม่มีให้ใช้มุมใกล้สุด ห้ามเดาด้านที่มองไม่เห็น\n- เอาเฉพาะตัวสินค้า ห้ามลอกฉากหลัง ของประกอบ ของตกแต่ง หรือข้อความโฆษณาในรูปมาใส่ฉาก\n"),
    ("กติกาเหล็ก — พื้นที่จริง (LOCATION LOCK): รูปพื้นที่ที่แนบมา (รูปห้อง/มุมบ้าน) คือสถานที่จริงของคลิปนี้ — ทุกช่องต้องเป็นพื้นที่นี้ ผนัง พื้น เฟอร์นิเจอร์ ตำแหน่งหน้าต่าง ทิศแสง ต้องตรงตามรูป ห้ามออกแบบห้องใหม่ เปลี่ยนได้เฉพาะสิ่งที่งานทำให้เปลี่ยน (สะอาดขึ้น เป็นระเบียบขึ้น ติดตั้งเสร็จ) · ช่องแรกของคลิป = สภาพตามรูปนี้\n",
     "กติกาเหล็ก — พื้นที่จริง: รูปห้องที่แนบคือสถานที่ของทุกช่อง ผนัง พื้น เฟอร์นิเจอร์ หน้าต่าง ทิศแสงตรงตามรูป ห้ามออกแบบห้องใหม่ เปลี่ยนได้เฉพาะสิ่งที่งานทำให้เปลี่ยน (สะอาดขึ้น เป็นระเบียบขึ้น ติดตั้งเสร็จ) · ช่องแรก = สภาพตามรูป\n"),
    (" · ตัวหนังสือ overlay ภาษาไทยต้องเรนเดอร์เป็นฟอนต์สไตล์ที่กำหนดจริงๆ ให้เห็นชัดเจน · สะกดถูกต้อง คมชัด อ่านง่าย · ห้ามใช้ emoji",
     " · ตัวหนังสือไทยตรงฟอนต์ที่กำหนด สะกดถูก คมชัด · ห้ามใช้ emoji"),
]
# จำนวนที่ต้องเจอ (นับจาก config ก่อนแก้): ต่อเนื่อง = mnBoard/mnBoard2 · LOCK = ทุกบอร์ด · ฟอนต์ = mnBoard 2 โหมด + บอร์ดอื่น 1
EXPECT = {0: 2, 1: 6, 2: 6, 3: 7}

boards = {}
def walk(n):
    if isinstance(n, dict):
        if str(n.get('id', '')).startswith('mnBoard') and n.get('type') == 'image': boards[n['id']] = n
        for v in n.values(): walk(v)
    elif isinstance(n, list):
        for v in n: walk(v)
walk(d)
assert sorted(boards) == ['mnBoard'] + [f'mnBoard{k}' for k in range(2, 7)], sorted(boards)

cnt = {}
def rep(x):
    if isinstance(x, str):
        for i, (a, b) in enumerate(REPL):
            if a in x: cnt[i] = cnt.get(i, 0) + x.count(a); x = x.replace(a, b)
        return x
    if isinstance(x, dict): return {k: rep(v) for k, v in x.items()}
    if isinstance(x, list): return [rep(v) for v in x]
    return x

for oid, op in boards.items():
    parts = op['prompt']['parts']
    tail = parts[-3:]
    assert isinstance(tail[0], str) and 'ปิดท้ายทุกภาพด้วย' in tail[0] and isinstance(tail[1], str) and 'คนในคลิป' in tail[1] and tail[2].get('table') == 'charBoard', (oid, tail)
    idx = next(i for i, p in enumerate(parts) if isinstance(p, dict) and p.get('op') == 'concat' and isinstance(p['parts'][0], str) and p['parts'][0].startswith('\n\n5 แถว:'))
    op['prompt']['parts'] = rep(parts[:idx] + tail + parts[idx:-3])

assert cnt == EXPECT, cnt
open(P, 'w', encoding='utf-8').write(json.dumps(d, ensure_ascii=False, indent=1) + ('\n' if raw.endswith('\n') else ''))
print('✓ showhow-dev.json v7 board-fit · replacements', cnt, '·', os.path.getsize(P), 'bytes')
